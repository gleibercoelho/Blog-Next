// lib/data.js
/* import connect from "./utils";


export default async function fetchPosts(req, res) {
  const {db} = await connect()

    // const user = {
    //    name: "Ned Stark"
    //} 
    const response = await db.collections("users").findOne("posts")

    return response

} */